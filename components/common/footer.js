import React from 'react';
import { View, Text } from 'react-native';

export default class Footer extends React.Component {
    render() {
        return (
            <View>
                <Text>Add Marvel vs DC competition on map of Berlin</Text>
            </View>
        );
    }
}
